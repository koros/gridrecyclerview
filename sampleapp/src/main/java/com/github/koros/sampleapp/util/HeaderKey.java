package com.github.koros.sampleapp.util;

/**
 * Semantic identifiers for the sample grid sections.
 */
public enum HeaderKey {
    /** Section containing actor cards. */
    ACTOR,

    /** Section containing director cards in richer sample data. */
    DIRECTOR,

    /** Section containing genre cards. */
    GENRE,

    /** Section containing movie cards. */
    MOVIE,

    /** Section containing studio cards in richer sample data. */
    STUDIO
}
